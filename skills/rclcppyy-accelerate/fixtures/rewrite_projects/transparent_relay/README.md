# Transparent Relay

`stock_app.py` is deliberately identical in the before and after cases. The Tier 0
rewrite is only compatible-profile activation in `run_case.py`. The oracle checks
ordered values, the exact stock object contract, stock-authority publisher and
subscription backend records, callback crossings, and stock-owned teardown.

The paired raw timing is a smoke artifact only. It includes four steady-state
messages after discovery and explicitly blocks performance claims.
